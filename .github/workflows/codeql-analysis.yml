name: "CodeQL"
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: '25 3 * * 3'
jobs:
    analyze:
        strategy:
            matrix:
            language: ['typescript']  # Remove 'javascript'
