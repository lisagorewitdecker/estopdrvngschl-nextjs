name: "CodeQL"
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
jobs:
    analyze:
        strategy:
            matrix:
            language: ['typescript', 'microsoft/typescript']  
